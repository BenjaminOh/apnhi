import Layout from "@/components/user/layout/Layout";

import UserPage from "./(user)/(pages)/page";

export default function Page() {
    return (
        <Layout>
            <UserPage />
        </Layout>
    );
}
